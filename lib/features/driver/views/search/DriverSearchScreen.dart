import 'package:flutter/material.dart';

class DriverSearchScreen extends StatelessWidget {
  const DriverSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Driver Search Screen"));
  }
}
